---
# ============================================================
# SECRETS — DEV (valeurs placeholder)
# À adapter avant usage en environnement réel.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # Doit être fourni au backend (JWT). Mettre une valeur >= 32 caractères.
  SECRET_KEY: "CHANGE_ME_DEV_SECRET_KEY_AT_LEAST_32_CHARS"
---
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  POSTGRES_DB: "myapp"
  POSTGRES_USER: "myapp_user"
  POSTGRES_PASSWORD: "rsjxEjf4KBDm7SDxsZ6m4tuJL+mBChf1165m69FX+YE="
  # Utilisé par le backend + initContainer db-migrate
---
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
type: Opaque
stringData:
  # Mot de passe Redis utilisé par le service Redis et le backend.
  REDIS_PASSWORD: "CHANGE_ME_REDIS_PASSWORD"
# ---
# apiVersion: v1
# kind: Secret
# metadata:
#   name: resend-credentials
# type: Opaque
# stringData:
#   # Clé API Resend pour l'envoi d'emails transactionnels.
#   RESEND_API_KEY: "re_auxGni84_PimKBJx7kjSBqXEzNHSQ36KL"
#   # Adresse expéditeur utilisée pour les emails de vérification.
#   RESEND_FROM_EMAIL: "M-Motors <no-reply@netdevops.fr>"
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
type: Opaque
stringData:
  # Identifiants d'accès MinIO (backend -> API S3 compatible).
  MINIO_ROOT_USER: "mmotors-dev-minio"
  MINIO_ROOT_PASSWORD: "mmotors-dev-minio-pass-change-me-2026"
